const port = process.env.PORT || 3000;

console.log("Running on port : ", port);

// "start": "node ./server/app.js  && next dev --port=$PORT",
// "start": "node ./server/app.js  && next dev -p $PORT",
